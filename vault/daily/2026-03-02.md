# 2026-03-02 — Daily Log

## Stripe App (com.datevbereit.export) — Investigation & Progress

### Root Cause Found
App was accidentally registered under **RADOM UG (Account A / acct_1T0zEqCL9DvLLy9E)** instead of Account B. The app ID is globally unique across all Stripe accounts — upload from Account B blocked as long as ghost record exists on Account A.

Confirmed via:
- `GET /v1/tailor/app_by_public_id` with Account B key → `"No such app"`
- Same endpoint with RADOM UG key → full app record, `merchant_id: acct_1T0zEqCL9DvLLy9E`

### Actions Taken
1. Arber uninstalled app from RADOM UG Dashboard → cleared `live_mode_version`
2. CC deleted build version `appver_61UCOMdXQk4Vmo15K41CL9DvLLy9EU80` via `DELETE /v1/tailor/app_build_version/{id}` — succeeded
3. No REST endpoint to delete app record itself → Stripe support ticket submitted
4. **Waiting for Stripe support** to delete ghost record from `acct_1T0zEqCL9DvLLy9E`

### Technical Notes (Stripe CLI internals)
- Tailor REST endpoints available: `GET app_by_public_id`, `POST/DELETE app_build_version`, `GET terms_acceptance`
- No DELETE endpoint for the app record itself
- Server file keyring has no directory configured → use `STRIPE_API_KEY` env var to bypass
- Interactive Y/N prompt can't be piped → use `pexpect` on server
- `is_terms_acceptance_valid = 'true'` in config.toml only works with `--project-name`, NOT `--api-key`
- Local: v1.0.5, Server: v1.0.4 at `/root/clawd/projects/stripe-datevbereit/datevbereit/`

### Next Step
Once Stripe support confirms deletion: run pexpect upload script on server (`/tmp/su4.py`) → re-upload to Account B.

---

## Ops

### JC Added to Docker Group
- `usermod -aG docker jc` on server `46.224.214.8`
- Confirmed: `jc : jc docker`

### Web Container Healthcheck Fix (facturx-pret + zugferd-bereit)
- **Problem:** `localhost` in wget healthcheck resolves to `::1` (IPv6) on some containers; Next.js listens on IPv4 only → healthcheck fails
- **Fix:** Changed `http://localhost:3000` → `http://127.0.0.1:3000` in `docker-compose.yml` for both apps
- Committed, pushed, pulled, and containers restarted on server
- Repos: `lamcearber-spec/Facturx-Pret` (commit `0f7a427`), `lamcearber-spec/ZUGFeRD-Bereit` (commit `d5c94e3`)
