# Daily Log — 2026-03-15 (Saturday)

## 04:55 UTC — peppol-ok.be Sprint Complete
- All 5 fixes LIVE: SSL, mobile nav, footer i18n, impressum, pricing page, IBAN fields
- JC implemented fixes around 22:14 UTC on 2026-03-14, rebuilt container, but didn't push to GitHub
- Max was checking stale GitHub clone (/tmp/) instead of live source (/root/clawd/projects/peppol-ok/) — caused unnecessary argument about IBAN fields existing
- Max fixed Impressum: HRB 132722 → 130877, added missing USt-IdNr DE367553843
- Rebuilt container, pushed commit 5b4d495 to GitHub
- Lesson: always check live server source, not just git clone

## 05:00–05:23 UTC — JC Auto-Responder Debugging + Fixes
- Arber asked why agent comms didn't work overnight
- **3 bugs found and fixed in comms system:**
  1. **Prompt injection rejection**: Claude CLI flagged persona prompt ("You are JC") as injection. Fix: rewrote to task-focused prompt ("A teammate sent this message requesting work on the server...")
  2. **Service crashes on timeout**: `set -euo pipefail` caused script exit when `timeout 300` returned exit code 124. Fix: removed `-e`, added explicit exit code handling
  3. **Discord message fragmentation**: Long messages from Max split into multiple Discord messages, each forwarded as separate Redis entries. Fix: added message batching in `comms-discord-poll` (merges consecutive messages from same author)
- Both services restarted: `comms-jc-responder`, `comms-discord-poll`
- Responder immediately picked up queued messages and processed them:
  - Arber's "2" → confirmed Prompt 2 status
  - Arber's "What's the status of prompt 2" → detailed status response
  - Arber's "Max paste the prompt again here" → wrote CC Prompt 3 (SEO & Infra, 5 items) saved to vault/projects/peppol-ok/cc-prompt-3-seo-infra.md
  - Max's Prompt 3 → currently being processed by responder (Claude CLI session active)
- **Files modified:**
  - `/usr/local/bin/comms-jc-responder` — removed `set -e`, task-focused prompt, explicit timeout handling
  - `/usr/local/bin/comms-discord-poll` — message batching (merge consecutive same-sender messages)
  - `/etc/systemd/system/comms-jc-responder.service` — CLAUDE_CODE_MAX_TURNS=10, removed ANTHROPIC_API_KEY
