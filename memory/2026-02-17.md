# 2026-02-17

## Voice Chat / Local AI Infrastructure (PINNED — resume later)

Arber wants to build a voice conversation pipeline so we can talk (speech-to-speech).

**Trigger:** Tweet about Voicebox (Qwen3-TTS local voice cloning app) — https://x.com/hasantoxr/status/2023444228018499846

**Options discussed:**
1. **Local Qwen3-TTS on server** — our Hetzner VPS has no GPU, won't work there

---

## Blog Posts — Fixed & Deployed
- **White screen bug:** German typographic quotes (`„"`) caused JSON hydration crash in Next.js. Fixed by replacing with straight quotes. Commit `3963719`.
- **Blog index mismatch:** `blog/page.tsx` linked to old CC-SPEC slugs that didn't match actual article slugs. Fixed in commit `096cd84`.
- **Correct slugs:** kontoauszug-datev-importieren, skr03-vs-skr04, stripe-shopify-datev-verbuchen, mt940-csv-camt-formate-erklaert, ki-buchhaltung-automatisieren
- **Sitemap:** Added `sitemap.xml` to `apps/web/public/`, live at datev-bereit.de/sitemap.xml. Commit `81e07c1`.

## Stripe Checkout Fix
- Error: `No such price: 'price_starter_monthly'` — frontend was sending placeholder strings
- Root cause: `NEXT_PUBLIC_STRIPE_PRICE_*` env vars were missing from `.env.datev`
- Added 3 monthly price IDs to `.env.datev` and rebuilt:
  - STARTER: `price_1T0zoFCL9DvLLy9EEsiWdK3U`
  - PRO: `price_1T0zr6CL9DvLLy9EJZYJYht6`
  - BUSINESS: `price_1T0ztUCL9DvLLy9EP0GHn7aH`
- **Still needed:** 3 yearly price IDs (prices exist in Stripe, Arber needs to copy the `price_1...` IDs)
- Business product: `prod_Tyxp0ce1b1mBob`, €99/mo + €800/yr

## Supermemory Plugin — PINNED (not yet activated)
- v1.0.5 installed at `/root/.openclaw/extensions/openclaw-supermemory`
- API key hardcoded in config
- Added to allowlist
- **Blocker:** Plugin is `kind: "memory"` — conflicts with memory-core for the memory slot
- Decision pending: need to set `plugins.slots.memory = "openclaw-supermemory"` to activate
- Config backed up at `/root/.openclaw/openclaw.json.backup-20260217-*`
- Arber said "put a pin on it" — resume when Stripe stuff is done

## Umami Analytics — Discussed
- Arber asked about web analytics for datev-bereit.de
- Recommended Umami (self-hosted, free, DSGVO-konform, no cookie banner) over Google Analytics
- Not yet set up — waiting for go-ahead
2. **Mac Mini M4** — Arber considering moving me to a Mac Mini (M4 Pro 24GB ~€1,049). Would enable local TTS, local LLM, no monthly costs. Keep Hetzner as lightweight reverse proxy.
3. **Cloud API route (cheapest/fastest):** Whisper (STT) + Claude (brain) + OpenAI TTS = ~$10-15/mo. Could set up same day.
4. **Groq** — friend recommended. Groq has fast Whisper STT ($0.02/hr) + fast LLM inference. Cheap but means using Groq models instead of Claude, or adding latency.

**Decision:** Pinned for later. No decision yet.

## datev-bereit.de Updates (deployed)
- **Domain redirect:** datevbereit.de → datev-bereit.de (301 via Squarespace forwarding)
- **Default locale:** Changed to German (localeDetection: false in middleware)
- **Blog articles:** Re-integrated 5 SEO articles from vault into codebase + committed to git
  - kontoauszug-datev-importieren, skr03-vs-skr04, stripe-shopify-datev-verbuchen, mt940-csv-camt-formate-erklaert, ki-buchhaltung-automatisieren
  - Articles had been written previously but never committed to repo; rebuild wiped them
- **Lesson:** Always commit content changes to git. Local-only Docker changes get wiped on rebuild.

**Key consideration:** Mac Mini = long-term local independence vs API route = quick & cheap to start.
