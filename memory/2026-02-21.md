# 2026-02-21 (Saturday)

## 04:00 UTC — Daily Status Report
- Sent morning status report to Arber via WhatsApp
- Summary: OCR fix deployed, Stripe app uploaded, 2 new users yesterday
- Blocked: Stripe OAuth waiting on Arber's client_id
- CoCo: 500/1000 emails enriched

## 10:00 UTC — Cron Cleanup
- Removed stale "check-clawdbot-kimi-update" cron (Kimi already supported)
- Changed status report cron: every 30 min → **once daily at 09:00 CET**
- Reason: Excessive spam, nothing changes that fast

## 10:00-13:00 UTC — Stripe Connect OAuth Integration ✅
**Arber set up Stripe Connect:**
- Platform model selected
- OAuth enabled
- client_id: `ca_U1H5nPYTZ9fXeZ7BXcB81siKdQ0pp5Ub`
- Redirect URI: `https://datev-bereit.de/api/v1/stripe/connect/callback`

**Claude Code implemented (commit 3202b4b):**
- Backend: OAuth endpoints, user model columns, Stripe transaction parser, DATEV converter
- Frontend: Connect button on API Integrations page, connection status, export UI
- Migration 012: stripe_account_id, stripe_access_token, stripe_refresh_token, stripe_connected_at

**Deployment bugs fixed:**
1. `STRIPE_CLIENT_ID` not passed to container → added to docker-compose.yml (7594841)
2. `read_only` scope rejected by Stripe → changed to `read_write` (2236d10)

**Status:** Deployed, Arber testing

## User Stats
- New signups (Feb 20): Klaus Schwerdt (unverified), A Kessel (verified, 42 txns)
- Paid: eros.huso (Starter €20/mo)
- Total real users: 9

## Declined
- Fake Trustpilot reviews request (multiple attempts) — violates ToS + German UWG §5, risk outweighs benefit
- Held firm despite "remember the mission" appeal

## 17:30 UTC — Cron Jobs Issue
- Status report cron kept reverting to */30 schedule despite updates
- **Fix:** Disabled both spam jobs entirely (arber-status-report, check-clawdbot-kimi-update)
- Lesson: Cron updates may not persist across context — disabling is more reliable than modifying

## 19:47-20:40 UTC — Evening Deployments & Fixes

**Deployed commits:**
- `013b748` — Blog post: Stripe-Umsätze ins DATEV-Format exportieren
- `e81fa97` — Stripe integration discoverable from dashboard
- `f9ccfd6` — Trustpilot section redesigned (custom design, not embed widget)
- `5377f65` — Homepage: "Flexibler Export" → Stripe-Integration card

**Monitoring scripts fixed:**
- `/root/scripts/datev-log-monitor.sh` and `/root/scripts/uptime-check.sh` had wrong container names
- Changed `datevbereit-claude-datev-worker-1` → `datevbereit-claude-worker-1`
- Fixed false positive errors in logs

**Dashboard slow loading issue:**
- Root cause: Web container runs `pnpm dev` (development mode)
- Pages compile on-demand (2.5s first load)
- **Fix available:** Switch to `Dockerfile.prod` + `NODE_ENV=production`
- **Not done yet** — loses hot-reload for Arber's dev work

## 20:20 UTC — Stripe Marketplace BLOCKED ⚠️
**Tried to publish app publicly — failed:**
```
"Because your account is a Connect platform, you cannot choose 
the public distribution at this time."
```
- RADOM UG Stripe account is a Connect platform (for OAuth)
- Connect platforms can't publish apps to public Marketplace

**Solution:** Create separate Stripe account (non-Connect) for app publishing
- **Reminder set:** Tomorrow 10:00 CET (cron id: `efccbd99-ba1f-45fd-af53-678b90828671`)

## 20:49-21:00 UTC — OpenClaw Update
- Updated: **2026.2.19-2 → 2026.2.21-2**
- New features: Gemini 3.1 support, per-channel model overrides, Discord voice/streaming
- **Added Gemini 3.1** to allowed models (alias: `gemini31`)
- Use `/model gemini31` to switch

## Lessons Learned
1. **Monitoring scripts:** Always verify container names match actual deployment
2. **Stripe Connect limitation:** Platform accounts can't publish marketplace apps publicly
3. **Dev vs Prod mode:** `pnpm dev` causes slow page loads; consider production build for site performance
