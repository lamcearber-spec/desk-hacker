# 2026-02-06

## Supermemory Plugin Issue & Fix Plan

**Problem discovered:**
- Supermemory plugin was causing me to go silent
- Root cause: It was overwriting `plugins.slots.memory` away from `"memory-core"`
- Arber spent 2 hours manually fixing the JSON to restore me

**Correct architecture (researched):**
- `plugins.slots.memory` MUST stay as `"memory-core"`
- Supermemory should run as an **additional** plugin via hooks
- It injects context before AI turns — doesn't need the memory slot

**Proper config:**
```json
{
  "plugins": {
    "slots": { "memory": "memory-core" },
    "entries": {
      "memory-core": { "enabled": true },
      "openclaw-supermemory": { "enabled": true, "config": {...} }
    }
  }
}
```

**Emergency rollback commands (save these):**
```bash
systemctl stop openclaw-gateway
cp ~/.openclaw/openclaw.json.backup-2026-02-06 ~/.openclaw/openclaw.json
systemctl start openclaw-gateway
```

**Status:** Plan created, waiting for Arber's approval to proceed with UAT.

## Discord Not Responding

Arber reported I wasn't answering Discord. Checked sessions — last response was at ~10:57 UTC. Unclear if I missed messages or connection issue. Need to monitor.

## Supermemory Fix (11:34 UTC)

Fixed Supermemory plugin to work alongside memory-core:
- Problem: Plugin declared `kind: "memory"` → competed for slot → I went silent
- Solution: Patched to remove kind field → runs as hook-based addon
- Email sent to Dhravya (CEO) requesting official addon mode
- Created protection: patch script, update wrapper, cron job every 6h
- All Supermemory features now working (auto-recall, auto-capture, tools)

Files:
- Patch script: `/root/clawd/scripts/patch-supermemory.sh`
- Update wrapper: `openclaw-update`
- Cron: `supermemory-patch-check` (every 6h)

## Rule: Always Use Claude Code for Coding (15:15 UTC)
**MEMORIZED:** Never write code directly. Always delegate coding tasks to Claude Code (cc).
- Use `pty:true` when running claude
- Use background mode for longer tasks
- Sub-agents with Kimi timeout on coding — use Claude Code instead

## Session Context (Pre-Compaction ~16:00 UTC)

### DatevBereit Launch Plan
- **Weekend (Feb 7-8):** Arber heavy testing
- **Sunday night:** Green light for Stripe integration
- **Monday Feb 9:** LAUNCH 🚀 + SEM/SEO strategy
- SSL cert reminder: April 20 (expires Apr 28)

### PayPal Feature (IN PROGRESS)
- Claude Code session: `plaid-zephyr` building PayPal CSV importer
- Git checkpoint: `pre-paypal-20260206-1556` (can revert with `git reset --hard pre-paypal-20260206-1556`)
- Arber requested: Always create git checkpoints before changes

### Marketing/Engagement Status
- **DATEV Community:** Logged in as "maddie", found relevant threads about CSV import pain. Editor interaction tricky — typed comment but submit button issues.
- **Reddit r/Finanzen:** Browsing works with Browserbase proxy, but login blocked by network security. Reminder set for 18:00 UTC for manual posting.
- Account: MadMaxInDaHaus

### Rules Reinforced
- **ALWAYS use Claude Code (cc) for coding** — Never write code directly
- **Create git checkpoints before changes** — For easy revert
