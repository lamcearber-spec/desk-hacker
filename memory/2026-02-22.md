# 2026-02-22 (Sunday)

## DatevBereit Outage Found & Fixed
- Discovered datev-api and datev-web were in a **restart loop** since Feb 20 evening
- Cause: old Docker containers (api-1, web-1, worker-1) holding ports 8000/3000, blocking new ones (datev-api-1, datev-web-1, datev-worker-1)
- **My fault** — during Feb 20 deploy (commit 213144f), service names changed but I didn't do `docker compose down` first
- Impact: ~46 hours of API downtime. Landing page was up (static) but uploads/conversions broken
- Arber spent €200 on Google Ads during this period — visitors couldn't convert
- Fixed at 18:26 UTC with full `down` + `up`
- Wrote outage report for Google Ads credit claim

## Killed lex-bereit containers
- lex-api and lex-worker were also restart-looping (port conflict with datev)
- Arber reminded me: Lexware export is integrated into DatevBereit site now, not separate
- This was already in MEMORY.md but I forgot — need to read more carefully

## Cron Zombie Problem (UNRESOLVED)
- Tried to kill all old crons and keep only datev-monitor (health + conversion check)
- Old "main" agent crons keep resurrecting from /root/.openclaw/cron/jobs.json
- Root cause: gateway writes in-memory state back to disk before restart, overwriting my file edits
- Can't fix from within — stopping gateway kills my own connection
- **TODO tomorrow: Ask Arber to run the stop/write/start sequence with a proper delay**
- The correct sequence: `openclaw gateway stop && sleep 5 && echo '{"version":1,"jobs":[]}' > ~/.openclaw/cron/jobs.json && openclaw gateway start`
- Then recreate datev-monitor via API after restart

## Config Changes
- Set `tools.exec.notifyOnExit: false` — exec errors no longer forwarded to WhatsApp
- Created datev-monitor cron (health check + conversion alerts every 15min)

## DatevBereit Stats
- 40 conversions last 7 days, 0 today/yesterday (outage)
- 7 real signups total, last signup Feb 20
- 1 paid subscriber (eros.huso, Starter plan)

## Texts Written for Arber
- DatevBereit company summary (200+ words, German) for a business directory
- Google Business description (170 words, German)
